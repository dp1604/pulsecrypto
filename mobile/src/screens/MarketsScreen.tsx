import { PlaceholderScreen } from "../components/PlaceholderScreen";

export const MarketsScreen = () => (
  <PlaceholderScreen
    title="Markets"
    subtitle="Watchlist, search, favourites, and live connection status will live here."
    note="Live backend data is not connected yet. The next phase will add REST metadata, WebSocket market batches, and persisted favourites."
  />
);
