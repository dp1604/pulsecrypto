import { PlaceholderScreen } from "../components/PlaceholderScreen";

export const TelemetryScreen = () => (
  <PlaceholderScreen
    title="Telemetry"
    subtitle="Optional performance and connection diagnostics placeholder."
    note="No telemetry metrics are collected yet. This screen is a lightweight shell for future assignment polish only."
  />
);
